#include <iostream>
using namespace std;

/*
템플릿 : 컴파일 단계에서 타입을 결정 짓는 기능이다.
타입이 다양하게 쓰일 수 있다.

template <typeName 원하는 이름> 의 형태로 사용가능하다.
template <class 원하는 이름> 의 형태로 사용 가능하다.

예) template <typename T>
void OutPut()
{
   cout << typeid(T).name() << endl;
}
*/

int main()
{
	return 0;
}
